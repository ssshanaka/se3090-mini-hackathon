# SuwaMaga

Client-side authentication UI for a university mini-hackathon prototype.

## Run the frontend

```bash
cd frontend
npm install
npm run dev
```

## Demo-only authentication notice

This phase uses browser `localStorage` for supervised demo authentication only. Production authentication must use a backend, hashed passwords, secure cookies, and proper authorization controls.
