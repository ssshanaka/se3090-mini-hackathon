import { Navigate } from 'react-router-dom';
import { useAuth } from '../context/AuthContext';
const residentCards = ['Report a Hazard', 'View Community Alerts', 'My Reports']; const phiCards = ['Pending Reports', 'Risk Triage', 'Verified Hazards', 'Resolved Cases'];
export default function DashboardPage() { const { user } = useAuth(); const isPhi = user.role === 'phi'; if (isPhi) return <Navigate to="/phi-dashboard" replace />; const cards = residentCards; return <section className="mx-auto max-w-6xl px-4 py-10 sm:px-6"><p className="text-sm font-bold tracking-widest text-brand-700 uppercase">{user.mohDivision} MOH Division</p><h1 className="mt-2 text-3xl font-extrabold tracking-tight text-slate-900">Resident Dashboard</h1><p className="mt-3 max-w-2xl text-slate-600">Welcome, {user.fullName}. These are temporary workspace shortcuts for this hackathon prototype.</p><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{cards.map((card) => <article key={card} className="rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="text-lg font-bold text-slate-900">{card}</h2><p className="mt-2 text-sm leading-6 text-slate-600">Coming in a future phase.</p></article>)}</div></section>; }
